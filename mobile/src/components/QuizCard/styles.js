import { StyleSheet } from "react-native";
export default StyleSheet.create({
  card: {
    borderRadius: 8,
    margin: 10,
  },

  number: {
    fontSize: 18,
    color: "#fff",
    textTransform: "capitalize",
  },

  result: {
    fontSize: 18,
    color: "#fff",
    textTransform: "capitalize",
  },

  category: {
    fontSize: 18,
    color: "#fff",
  },
});
